https://localhost {
	tls internal

	# Proxy MCP server running on localhost:8000 (for example)
	reverse_proxy 127.0.0.1:8080

	header {
		Strict-Transport-Security "max-age=31536000;"
	}

	log {
		output file caddy.log
	}
}
